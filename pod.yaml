# pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
    - name: my-container
      image: nginx:latest
      ports:
        - containerPort: 80
# added a comment to indicate that this is a simple pod configuration for an Nginx container.
# this is a simple pod configuration for an Nginx container.
# thoer comment added to indicate that this is a simple pod configuration for an Nginx container.